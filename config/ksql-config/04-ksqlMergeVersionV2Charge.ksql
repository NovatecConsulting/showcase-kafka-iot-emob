INSERT INTO wallbox_charge
SELECT
     CLIENT_ID_KEY,
     CLIENT_ID,
     PROTOCOL_TRANSLATION(STATUS,EVENT,ACTION,AMOUNT,MAX,RESPONSE_AMOUNT,RESPONSE_MAX) AS CHARGE,
     TIMESTAMP
FROM wallbox_V2
WHERE
     MQTT_TOPIC='charge';
