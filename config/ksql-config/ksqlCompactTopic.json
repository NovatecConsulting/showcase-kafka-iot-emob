{
    "ksql": "CREATE TABLE \"CIQ000000017_out_charge_compacted\" AS SELECT CLIENT_ID, LATEST_BY_OFFSET(CHARGE) AS CHARGE FROM \"CIQ000000017_out_charge\" GROUP BY CLIENT_ID EMIT CHANGES;",
    "streamsProperties": {
          "ksql.streams.auto.offset.reset": "earliest"
    }
}
  
