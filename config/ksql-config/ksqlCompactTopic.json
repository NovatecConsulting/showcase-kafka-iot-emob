{
    "ksql": "CREATE TABLE \"wallbox_chargestatus\" AS SELECT CLIENT_ID, LATEST_BY_OFFSET(CHARGE) AS CHARGE FROM \"wallbox_charge\" GROUP BY CLIENT_ID EMIT CHANGES;",
    "streamsProperties": {
          "ksql.streams.auto.offset.reset": "earliest"
    }
}
  
