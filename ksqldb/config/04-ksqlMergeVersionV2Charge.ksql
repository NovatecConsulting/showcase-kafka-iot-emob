INSERT INTO wallbox_charge__filter
SELECT
     CLIENT_ID_KEY,
     CLIENT_ID,
     PROTOCOL_TRANSLATION(STATUS,EVENT,ACTION,AMOUNT,MAX,RESPONSE_AMOUNT,RESPONSE_MAX) AS CHARGE,
     TIMESTAMP
FROM wallbox__v2__filter
WHERE
     MQTT_TOPIC='charge';
